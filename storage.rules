rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read access for storage files
    match /{allPaths=**} {
      allow read: if true;
    }

    // Only authenticated users can write/upload
    match /{allPaths=**} {
      allow write: if request.auth != null;
    }
  }
}
